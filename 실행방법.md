# 🚀 프로젝트 실행 방법

이 프로젝트는 Python 백엔드(FastAPI)와 Next.js 프론트엔드로 구성된 풀스택 애플리케이션입니다.

## 📋 사전 준비

### 1. Python 의존성 설치

```bash
# uv 사용 (권장)
uv sync

# 또는 pip 사용
pip install -r requirements.txt
```

### 2. Node.js 의존성 설치

```bash
cd frontend
npm install
```

### 3. 환경 변수 설정

#### 백엔드 환경 변수 (프로젝트 루트에 `.env` 파일 생성)

```env
# OpenAI API 키 (필수)
OPENAI_API_KEY=your-openai-api-key-here

# Supabase 설정 (선택사항)
SUPABASE_URL=https://your-project-id.supabase.co
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# 프론트엔드 URL (CORS 설정용)
FRONTEND_URL=http://localhost:3000
```

#### 프론트엔드 환경 변수 (`frontend/.env.local` 파일 생성)

```env
# Supabase 설정
NEXT_PUBLIC_SUPABASE_URL=https://your-project-id.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key-here

# 백엔드 URL
NEXT_PUBLIC_API_URL=http://localhost:8000
NEXT_PUBLIC_PYTHON_BACKEND_URL=http://localhost:8000
```

## 🎯 실행 방법

### 방법 1: PowerShell 스크립트 사용 (Windows 권장)

#### 백엔드 실행

```powershell
# 프로젝트 루트에서 실행
.\start_api.ps1
```

또는

```powershell
uv run python api_server.py
```

#### 프론트엔드 실행

```powershell
cd frontend
npm run dev
```

### 방법 2: 수동 실행

#### 백엔드 실행

```bash
# 프로젝트 루트에서 실행
python api_server.py

# 또는 uvicorn 직접 사용
uvicorn api_server:app --reload --port 8000
```

백엔드 서버가 시작되면:
- 서버 주소: http://localhost:8000
- API 문서: http://localhost:8000/docs

#### 프론트엔드 실행

```bash
cd frontend
npm run dev
```

프론트엔드 서버가 시작되면:
- 웹 애플리케이션: http://localhost:3000

## 🔄 전체 실행 순서

1. **터미널 1**: 백엔드 실행
   ```powershell
   .\start_api.ps1
   ```

2. **터미널 2**: 프론트엔드 실행
   ```powershell
   cd frontend
   npm run dev
   ```

3. 브라우저에서 http://localhost:3000 접속

## 📝 참고사항

- 백엔드는 포트 **8000**에서 실행됩니다
- 프론트엔드는 포트 **3000**에서 실행됩니다
- `.env` 파일이 없으면 스크립트가 오류 메시지를 표시합니다
- 환경 변수 변경 후에는 서버를 재시작해야 합니다

## 🛠️ 문제 해결

### 백엔드가 시작되지 않는 경우

1. `.env` 파일이 프로젝트 루트에 있는지 확인
2. `OPENAI_API_KEY`가 올바르게 설정되었는지 확인
3. Python 의존성이 모두 설치되었는지 확인 (`uv sync` 또는 `pip install -r requirements.txt`)

### 프론트엔드가 시작되지 않는 경우

1. `frontend/.env.local` 파일이 있는지 확인
2. `npm install`이 완료되었는지 확인
3. 포트 3000이 이미 사용 중인지 확인

### CORS 오류가 발생하는 경우

백엔드 `.env` 파일에 `FRONTEND_URL=http://localhost:3000`이 설정되어 있는지 확인하세요.
